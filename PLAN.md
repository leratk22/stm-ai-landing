# STM AI Landing — план вёрстки

**Макет:** Figma «Студия STM» → [node 63:887](https://www.figma.com/design/vGlmrJDWDghAAjnVBGrCZ9/Hypothesis?node-id=63-887)
**Стилевой референс:** https://atlasdesign.framer.ai/
**Стек:** Astro 7 + Tailwind 4, TypeScript strict, Node 22 (см. `.nvmrc`)

---

## Что взято с референса Atlas

Разобрали живую страницу — вот что воспроизводим и чем заменяем:

| Приём | Как у Atlas | Как у нас |
|---|---|---|
| Градиентный фон героя | WebGL2-шейдер на `<canvas>` во всю секцию | свой фрагментный шейдер без библиотек — `src/scripts/gradient.ts` |
| Появление блоков | Framer Motion, `opacity 0→1` + `translateY 20–30px`, стаггер | CSS-переход + один IntersectionObserver — `src/scripts/reveal.ts` |
| Бегущая строка | JS-трансформ `translateX` | CSS `@keyframes` + дублированный трек |
| Липкие блоки | 4 sticky-контейнера | нативный `position: sticky` |
| Палитра | `#0C0E12` фон, `#111317` поверхность, `#FF4112` акцент | акцент берём из макета — `#FF1E00` |
| Типографика | Golos Text 600, трекинг −0.06em | шрифт макета — Inter 500/700 |

Принцип: та же пластика, но без рантайма Framer Motion — на лендинге он не окупается.

---

## Этапы

### 0. Подготовка — ✅ сделано
- [x] Каркас Astro + Tailwind, Node 22
- [x] Токены дизайна в `@theme` (`src/styles/global.css`)
- [x] Шейдерный градиентный фон (WebGL2 + CSS-фолбэк)
- [x] Утилиты движения: reveal, marquee
- [x] **Выгрузка ассетов из Figma** — разблокирована 16.09.2026
      (нужны были и Allowed directories, и активная вкладка с файлом дизайна)
      Выгружено: `logo-stm.svg`, `case-coffee-mood.png`, `hero-gradient.png`, линия, точка.

### 1. Секции — по порядку сверху вниз

| # | Секция | Figma node | Статус |
|---|---|---|---|
| 1 | Header + Hero + слайдер кейсов | `146:567` | ✅ вёрстка, слайдер не подключён |
| 2 | Бегущая строка | — | ✅ |
| 3 | «Объединили студийный дизайн-опыт» | `146:634` | ✅ сверено по Figma |
| 4 | «Кому подойдёт» (01/02/03) | `163:93` | ✅ сверено по Figma |
| 5 | Таблица сравнения | `147:851` | ✅ сверено по Figma |
| 6 | «Что включено в проект» | `163:132` | ✅ сверено по Figma |
| 7 | «Как проходит работа» — 6 шагов | `163:235` | ✅ сверено по Figma |
| 8 | Кейсы / экспертиза | `163:319` | ✅ сверено по Figma |
| 9 | CTA «Создадим сайт за 390 000 ₽» | `165:539` | ✅ сверено по Figma |
| 10 | FAQ-аккордеон | `165:561` | ✅ сверено по Figma |
| 11 | Юридическая строка | `165:583` | ✅ сверено по Figma |

### 2. Адаптив
Брейкпоинты 1728 → 1440 → 1024 → 768 → 390. Мобильных макетов нет, решения наши.
Требуют отдельного подхода:
- таблица сравнения на 5 колонок → горизонтальный скролл или карточки;
- сетка из 6 шагов → одна колонка;
- бургер-меню вместо горизонтальной навигации.

### 3. Интерактив
- [x] Слайдер кейсов в герое — автопрокрутка с полосой прогресса
- [x] FAQ-аккордеон — нативный `<details>`, без JS
- [ ] Бургер-меню
- [ ] Липкий хедер при скролле
- [ ] Параллакс крупных блоков

### 4. Полировка и деплой
- [ ] Картинки в WebP + lazy-load
- [x] Самохостинг Golos Text вместо Google Fonts — вариативный woff2, cyrillic + latin, 64 КБ, preload кириллицы
- [ ] Метатеги, OG-картинка, `sitemap.xml`
- [ ] Lighthouse
- [ ] GitHub + деплой

---

## Открытые вопросы к копирайту

В макете часть текстов — плейсхолдеры, дублируются между блоками:
- «Как проходит работа»: у шагов 2, 3, 5, 6 стоит один и тот же текст
  «Классическая разработка требует времени и большого…»
- «Объединили студийный дизайн-опыт»: у обеих колонок текст обрезан в макете

Нужен финальный копирайт.


---

## Замеры и находки по макету

Точные значения, снятые из Figma (не с референса):

| Параметр | Значение |
|---|---|
| Акцент (кнопки) | `#FF1E00` |
| Поверхность (стрелки слайдера) | `#111317` |
| Шрифт | Inter, 500 и 700 |
| Заголовок героя | 128px / 700 / line-height 0.86 / tracking −6.4px |
| Лид | 28px / 500 / 1.2 / −0.56px |
| Мелкие капсы | 12px / 500 / −0.24px / прозрачность 70% |
| Кнопка | высота 50px, padding 26/14, радиус 100px, текст 16px |
| Карточка кейса | 414×220, фон `rgba(255,255,255,0.15)`, обводка `rgba(255,255,255,0.25)` |
| Сетка героя | хедер 36px, лид и карточка 164px, H1 485px, линия 733px, строка 761px |

### Градиент героя

В макете он экспортирован растром 1728×982 (`src/assets/hero-gradient.png`, 2 МБ).
Пиксельный замер показал геометрию: красный держится **вдоль всего правого края**
(x=0.5 → `#600000`, x=0.75 → `#DD100D`, x=1 → `#FF3314`), плюс тёплый клин
в левом нижнем углу (`#FF6D1C`), база — чистый чёрный.

Шейдер построен по этим числам, PNG в загрузку не идёт. Статичный CSS-фолбэк
под канвасом собран по тем же опорным точкам.

### Требует решения

1. **Красный в заголовках — это уже система, а не оплошность.**
   В макете `#FF4112` встречается в логотипе и в акцентах обоих
   свёрстанных заголовков («AI-разработки», «сильное впечатление»),
   а `#FF1E00` — только на кнопках. Два из двух заголовков, то есть
   в макете разделение осознанное: `#FF4112` для текстовых акцентов,
   `#FF1E00` для кнопок.
   Сейчас в коде везде `#FF1E00` по решению от 17.09.2026 — то есть мы
   сознательно расходимся с макетом. Требует подтверждения.
2. **Бегущая строка.** В макете это статичный ряд, разложенный по ширине.
   Сделана анимированной — как на референсе. Если нужно статикой, скажи.
3. **Линия-разделитель и точки** выгружены из Figma как SVG, но собраны
   на CSS (`border` и `border-radius`) — это чистая геометрия, растить
   на ней запросы смысла нет. Логотип, наоборот, встроен из экспорта.


---

## Переделать

- **Градиентный шейдер героя не нравится заказчику** (17.09.2026).
  Отложено, макет пока остаётся. Варианты на обсуждение: другой характер
  движения, статичный экспорт из Figma вместо шейдера, либо CSS-градиент
  с лёгким дрейфом. Нужно уточнить, что именно не устраивает —
  цвет, форма пятна или сама анимация.

## Заметки по вёрстке

- **`hidden` не работает на компоненте `Button`.** В базе кнопки стоит
  `inline-flex`, и Tailwind ставит его в CSS позже `hidden` — класс
  снаружи проигрывает независимо от порядка в атрибуте. Прятать кнопку
  нужно обёрткой (`<div class="hidden sm:block">`), а не классом на ней.
- **SVG из Figma нельзя отдавать в `<Image>`.** Экспорт идёт с
  `preserveAspectRatio="none"` и жёсткими `width`/`height`; Astro возвращает
  битую картинку (`naturalWidth: 0`). Логотип встроен инлайном через `?raw`.


---

## Сверка второй секции с Figma (17.09.2026)

Секция сначала была собрана по габаритам и скриншоту, потом сверена
с `get_design_context`. Габариты совпали, значения — нет. Что исправлено:

| | было на глаз | в макете |
|---|---|---|
| Фон секции | `#0C0E12` | `#111317` |
| Фон карточек | `black/45` | `#000000` |
| Фон тегов | `white/8` | `#111317` |
| H2 | 44px / 600 / 1.2 | 48px / 700 / 1.1 |
| Лид | 24px / 1.42 / белый 56% | 28px / 400 / 1.2 / белый |
| Заголовок карточки | 24px / 500 | 24px / 600 |
| Текст карточки | 16px / 400 / 1.56 / белый 56% | 18px / 300 / 1.4 / белый 50% |

Отдельно: в тексте карточек **Inter Light (300)**, а шрифт был скачан
в диапазоне 400–700 — браузер подделывал начертание. Перекачан на 300–700.

**Вывод на будущее:** габариты боксов из `get_metadata` позволяют собрать
раскладку, но кегли, начертания и цвета из них не выводятся. Секции без
`get_design_context` считать незаконченными.


## Сверка «Кому подойдет» (163:93)

Секция собрана сразу из `get_design_context` и сверена числом на 1728.
Совпало всё:

| | Макет | Факт |
|---|---|---|
| Фон | чёрный | `rgb(0,0,0)` |
| Высота секции | 521 | 522 |
| Паддинги | 146 / 112 | 112 по вертикали, контейнер по горизонтали |
| Разрыв заголовок ↔ список | 96 | 96 |
| Заголовок | 48 / 700 / 1.1 / −0.96, блок 410 | 48 / 700 / 52.8 / −0.96, 410×264 |
| Пилюля индекса | 36×23, `#111317`, 12 / 500 / −0.24 | идентично |
| Заголовок строки | 28 / 400 / 1.2 / −0.56, блок 430 | 27.99 / 400 / 33.59 / −0.56, 430×34 |
| Описание | 18 / 300 / 1.4 / −0.576, белый 50% | 17.97 / 300 / 25.16 / −0.58 |
| Паддинг строки | 24 | 24 |
| Разрывы в строке | 32 | 32 |

**Странность макета:** у первой строки разделитель `rgba(17,17,17,0.18)`
— почти невидимый на чёрном, — а у второй и третьей `rgba(255,255,255,0.1)`.
Оставлено как в оригинале; выглядит как недосмотр.


---

## Правило: не смешивать текучий кегль с фиксированными колонками

Ошибка, найденная 17.09.2026 по замечанию заказчика («мельчишь в текстах,
и колонка описания узкая»). Обе проблемы шли от одного:

- кегли были заданы `clamp()` от `vw` — ужимались вместе с окном;
- ширины колонок стояли в пикселях макета (410 под заголовок, 430 под
  название пункта) — не ужимались.

Ниже 1728 это расходилось. На 1280 заголовок падал до 35.6px, а колонка
описания — до 196px вместо 400.

**Как теперь:**

1. **Кегли — ступенями по брейкпоинтам, не `clamp` от `vw`.**
   С 1024px держим ровно размеры макета: H2 48, лид 28, заголовок карточки 24,
   текст 18. Ниже — две ступени вниз. Исключение — дисплейный заголовок героя:
   он по замыслу тянется во всю ширину, там текучесть уместна.
2. **Колонка, задающая переносы в заголовке, остаётся в пикселях макета.**
   410px — тогда заголовок ломается на те же 5 строк, что в оригинале.
3. **Колонки, которые просто делят остаток, — в долях.**
   Название пункта 46.24% списка вместо 430px.

Проверка после правки (заголовок 48px и 5 строк на всех трёх):

| Ширина | Название пункта | Описание |
|---|---|---|
| 1728 | 430 | 400 |
| 1440 | 410 | 376 |
| 1280 | 336 | 290 |


## Брейкпоинты: не ставить порог на популярной ширине

Порог двухколоночной раскладки в «Кому подойдет» стоял на 1280. Окно
заказчика — 1264, то есть на 16px ниже: обычный десктоп получал раскладку
в одну колонку. Порог опущен до **1152**.

Пробовал 1024 — не подошло: под заголовок остаётся ~320px и он рвётся
на 7 строк вместо макетных 5.

Итоговая логика секции:

| Ширина | Раскладка | Заголовок | Разрыв | Описание |
|---|---|---|---|---|
| ≥ 1484 | в строку | 410, 5 строк | 96 | 400 (как в макете) |
| 1280–1483 | в строку | 410, 5 строк | 96 | 308–400 |
| 1152–1279 | в строку | 410, 5 строк | 48 | 247–308 |
| < 1152 | в колонку | во всю ширину | — | во всю ширину |

Колонка заголовка везде 410px: от неё зависят переносы, и только так
заголовок ломается на те же строки, что в оригинале. Ужимается разрыв.

**Проверять каждую секцию на:** 1728 (макет), 1512, 1264 (окно заказчика),
1152 и 1140 (по обе стороны порога), 375. Горизонтального скролла быть
не должно ни на одной.


## Сверка таблицы сравнения (147:851)

Собрана из `get_design_context`, семантическим `<table>` с `<th scope>`.

| | Макет | Факт |
|---|---|---|
| Ширина таблицы | 1436 | 1436 |
| Колонки | 287.2 × 5 | 287 × 5 |
| Высота шапки | 43 | 43 |
| Ячейка | padding 18/12, 14px / 1.3 | идентично |
| Шапка и футер STM | `#FF1E00` | `rgb(255,30,0)` |
| Колонка STM в строках | `#280F0F` / `#341313` | идентично |
| Цены конкурентов | `#FF4112` | `rgb(255,65,18)` |

**Расхождение:** строка «Техническая оснащённость» — 61px против 79 в макете.
В Figma длинная ячейка ложится в 3 строки, в браузере при той же ширине
колонки (251px под текст) — в 2. Высоты строк намеренно отданы содержимому:
фиксированные 43/61/79 из макета начали бы резать текст, как только
колонка станет уже 287px.

### Адаптив: карточка на параметр

Первый вариант — горизонтальный скролл с липкой первой колонкой — забракован
(17.09.2026): на лендинге скролл вбок легко не заметить, и из пяти колонок
одновременно видно две, то есть сравнение разваливается.

Рассматривались четыре варианта:

| Вариант | Почему не взяли |
|---|---|
| Переключатель конкурента (STM + один выбранный) | три четверти данных за кликом |
| Карточка на участника (4 карточки по 9 строк) | пропадает построчное сравнение — смысл таблицы |
| Скролл с прилипанием, точками и градиентом у края | скролл остаётся |
| **Карточка на параметр** | **выбран** |

Как сделано: с 1024px обычная таблица, ниже — карточка на каждый параметр.
Внутри карточки все четыре участника списком, STM выделен подложкой `#280F0F`
и полужирным названием. Цена вынесена отдельной карточкой: STM на `#FF1E00`
белым, конкуренты — `#FF4112`.

Правки по замечаниям (17.09.2026):

- строки разделены хейрлайнами `border-white/10`;
- строки вынесены на всю ширину карточки (`-mx-4 px-4`) — разделитель идёт
  от края до края, подложка строки STM не висит островом внутри отступов;
- поля ужаты: у строк был собственный `px-3 py-2` поверх padding карточки,
  текст отступал на 28px от края; теперь отступ один — 16px;
- заголовок параметра поднят с 16px до **`type-h3`** (20px на узких) —
  тот же стиль, что у карточек второй секции, новый кегль не заводился;
- добавлен счётчик 01–08 справа на одном уровне с заголовком, пилюлей того
  же вида, что индексы в «Кому подойдет». Только в мобильной вёрстке:
  в таблице порядок задаёт сама строка, а разрозненным карточкам он нужен.
  На карточке цены номера нет — это итог, а не очередной параметр;
- подложка строки STM в карточках заменена с `#280F0F` на
  `rgba(255,255,255,0.09)`: тёмно-красный из таблицы на маленькой площади
  читался грязным. Красный в карточках остался только на цене.
  **В таблице `#280F0F` / `#341313` не тронуты** — там площадь большая
  и цвет работает;
- добавлен заголовок блока `«Сравнение с другими подходами»` (`type-h2`,
  28px), только в мобильной вёрстке. Без шапки таблицы было непонятно,
  что сравнивают карточки. **В макете этого заголовка нет — формулировка
  наша, требует утверждения.**

Ничего не скрыто, горизонтального скролла нет вообще — ни у таблицы,
ни у страницы. Разметка разная для двух режимов (`<table>` и `<dl>`),
скрытая ветка не читается скринридером.

Проверено на 1728 / 1264 / 1024 / 1000 / 375:

| Ширина | Что показано | Скролл страницы |
|---|---|---|
| 1728 | таблица 1436, колонки 287×5 | нет |
| 1264 | таблица 1216 | нет |
| 1024 | таблица 976 | нет |
| 1000 | карточки | нет |
| 375 | карточки | нет |


## Сверка «Что включено в проект» (163:132)

Собрана из `get_design_context`, сверена числом на 1728 — совпало всё:

| | Макет | Факт |
|---|---|---|
| Высота секции | 475 | 476 |
| Отступ сверху / разрыв | 112 / 56 | 112 / 56 |
| Заголовок | 48 / 700 / 1.1 / −0.96 | идентично |
| Примечание | 18 / 300 / 1.4 / −0.576, блок 408, по правому краю | идентично |
| Ячейка | 478×116, padding 24/20, разрыв 20 | 479×116 |
| Индекс | 12 / 500 / −0.24, колонка 32 | идентично |
| Заголовок пункта | 24 / 600 / 1.2 / −0.48 | идентично |
| Границы | `rgba(255,255,255,0.1)` сверху, боковые у средней колонки | идентично |

Новых стилей не заводил: заголовок `type-h2`, примечание `type-card-body`,
пункт `type-h3`, индекс `type-eyebrow`.

**Индекс здесь — обычный текст в колонке 32px, а не пилюля** как в
«Кому подойдет» и в карточках таблицы. Так в макете.

### Адаптив

Сетка 3 → 2 → 1 колонки. Боковые границы навешиваются через `nth-child`
в scoped-`<style>`, а не классами: при смене числа колонок меняется и то,
какая ячейка первая в ряду, и классами это пришлось бы переопределять
на каждом брейкпоинте.

Примечание справа по правому краю только на десктопе; ниже — обычным
текстом слева под заголовком.

Проверено на 1728 / 1264 / 768 / 375 — горизонтального скролла нет нигде,
границы на каждом режиме встают по первому столбцу верно.


## Правило: структурные линии не анимируем

Ошибка, найденная 17.09.2026 по замечанию заказчика («даже у пустой
секции должны быть полосочки»). Граница ячейки висела на том же
элементе, что и класс `.reveal`. Пока ячейка не появилась, она стоит
на `opacity: 0` — а прозрачность гасит и границу. Сетка выглядела
оборванной: последняя ячейка «Что включено в проект» читалась как
пустая дыра без линий.

**Как теперь:** границы и разделители — на статичной обёртке,
`.reveal` — на содержимом внутри. Каркас секции виден всегда,
анимируется только то, что в нём лежит.

Исправлено в:
- `Scope.astro` — границы ячеек сетки;
- `Audience.astro` — разделители строк списка.

В `Advantages.astro` проблемы не было: вертикальные линии там
отдельным компонентом `GridLines`, горизонтальный разделитель — вне
`.reveal`. В карточках таблицы тоже нет: карточка появляется целиком
как блок с подложкой.


## Сверка «Как проходит работа» (163:235)

| | Макет | Факт |
|---|---|---|
| Фон | `#111317` | `rgb(17,19,23)` |
| Высота секции | 1129 | 1129 |
| Заголовок | 48 / 700 / 1.1 / −0.96 | идентично |
| Лид | 28 / 400 / 1.2 / −0.56 | идентично |
| Карточка | 473×360, padding 20/20/32 | 473×360, идентично |
| Пилюли | номер + срок, разрыв 4 | идентично |
| Заголовок карточки | 24 / 600 / 1.2 / −0.48 | идентично |
| Текст | 18 / 300 / 1.4 / −0.576 | идентично |
| Хейрлайны | 138 / 624 / 1103 / 1590 | 138 / 625 / 1103 / 1589 |

### Рефакторинг

Карточка «пилюли сверху, заголовок и текст снизу» встречается и здесь,
и во второй секции — вынесена в `ui/InfoCard.astro`. `Advantages.astro`
переведён на неё же.

Попутно исправлено: разрыв между пилюлями во второй секции был 6px,
в макете 4px. Теперь у обеих секций 4px.

`ui/GridLines.astro` получил параметр `dividers` — сколько внутренних
линий рисовать (1 для сетки в две колонки, 2 для трёх). Ниже lg сетка
перестраивается и внутренние линии перестают совпадать с разрывами,
поэтому там они скрыты, остаются только крайние.

Проверено на 1728 / 1264 / 375. Сетка 3 → 2 → 1 колонки,
горизонтального скролла нет.


## Сверка «Мы обладаем серьёзной экспертизой» (163:319)

Фон — тот же градиент, что в герое; переиспользован `GradientBackdrop`.

**Превью не заливают карточку целиком** — каждое вписано по-своему,
размеры из макета (карточка 473×436):

| Кейс | Размер | Как посажен |
|---|---|---|
| Телефон | 293×600 | прижат к верху на 20px, обрезан снизу |
| Ноутбук | 634×422 | шире карточки, обрезан по бокам, прижат к низу |
| Дашборд | 435×309 | помещается целиком, по центру |

Первая версия была с `object-cover` во всю карточку — картинки резались
не там, где задумано. Переделано на доли от карточки.

Нижняя полоса: в макете статична, но пара фактов в ней повторяется
дважды, то есть задумана бегущей строкой. Пущена как в герое.
`Marquee.astro` параметризован (`items`, `duration`, рамка, отступы)
и используется теперь в двух местах.

### Ассеты

Figma повторно отдала градиент и кофейный кейс — дубли удалены
(сверено по md5). Осталось 4 уникальных растра. Новые в WebP:
ноутбук 1443 КБ → 58 КБ, дашборд 239 КБ → 142 КБ.

### Правка адаптива

Жёсткий перенос строки из макета (`whitespace-pre-line`) переведён
в `lg:` — на узких экранах он давал рваные строки. Сделано и здесь,
и во второй секции.

Проверено на 1264 / 375 — горизонтального скролла нет, сетка 3 → 2 → 1.


## Сверка CTA / FAQ / подвала

| | Макет | Факт |
|---|---|---|
| CTA: высота | 567 | 568 |
| CTA: заголовок | 108 / 700 / 0.86 / −4.32, блок 952 | идентично, блок 952 |
| CTA: правая колонка | 472 | 472 |
| CTA: поля | 146 / 144, граница сверху `white/8` | идентично |
| FAQ: высота | 376 | 372 |
| FAQ: колонки | 718 / 718 | 718 / 718 |
| FAQ: вопрос | 28 / 400 / 1.2 / −0.56, поля 16 | идентично |
| FAQ: граница | `rgba(255,255,255,0.15)` | идентично |
| Подвал: высота | 81 | 81 |
| Подвал: текст | 18 / 300 / 1.4 / −0.576, белый 50% | идентично |

Поймана ошибка: в CTA и FAQ я оставил `gap` между колонками, которого
в макете нет — доли уже учитывают промежуток. Из-за лишних 48px flex
ужимал колонки (заголовок 928 вместо 952). Убрано.

Добавлен `.type-display-md` — 108px. Здесь кегль **текучий**, в отличие
от заголовков секций: блок под него задан долей контейнера (952 из 1436),
и на ступенях текст перестал бы попадать в свои две строки.

### FAQ

Собран нативным `<details>` — работает без JS, доступен с клавиатуры,
плюс поворачивается в крестик через `group-open:rotate-45`.

**В макете нарисовано только свёрнутое состояние — текстов ответов там нет.**
Ответы в `content.ts` собраны из копирайта других секций (преимущества
и этапы), ничего не выдумано, но **требуют утверждения**.

### Полоса фактов в кейсах

Сначала была сделана бегущей строкой — исправлено на статику,
разнесённую по всей ширине, как в макете (17.09.2026).

---

## Все 11 секций свёрстаны

Проверка всей страницы на 375: горизонтального скролла нет ни у страницы,
ни у одной секции.

### Что осталось

1. **Шейдер в герое** — заказчику не нравится, ждём уточнения.
2. **Красный в текстовых акцентах** — `#FF1E00` против макетного `#FF4112`.
3. **Тексты ответов FAQ** — утвердить или заменить.
4. **Заголовок блока сравнения на мобильном** — формулировка наша.
5. Слайдер кейсов в герое, бургер-меню, липкий хедер.
6. Метатеги, OG-картинка, Lighthouse.


## Заголовок CTA: две фразы вместо одной (17.09.2026)

По замечанию заказчика. В копирайте это два предложения
(«…за 390 000 ₽.» и «Обсудим?»), а при интерлиньяже 0.86 строки идут
почти вплотную, и короткий хвост читался как случайный обрыв,
а не как отдельная реплика — вёрстка спорила с текстом.

Разведено на два блока внутри одного `<h2>`. Отступ задан в `em`
(0.34em), чтобы держать пропорцию к кеглю на любой ширине.

Замер на 1264 (кегль 79px):

| | |
|---|---|
| Шаг между строками | 68px |
| Шаг до «Обсудим?» | 95px |
| Отношение | 1.4× |

Первая версия была 0.2em — почти не читалась на фоне шага 68px.

Итоговый вариант (после правок заказчика): горизонтальный сдвиг убран,
вертикальный отступ сокращён до `0.14em` (11px при кегле 79 против шага
строки 68), **реплика выкрашена в акцентный цвет**. Отделяет её теперь
цвет, а отступ только не даёт строкам слипнуться.

Первые версии: 0.2em (не читалось), 0.34em + сдвиг 0.45em (перебор).


## Стеклянные плашки под превью (17.09.2026)

По замечанию заказчика. Изменено у всех четырёх плашек — три в кейсах
и одна в герое, это один и тот же элемент:

| | Было | Стало |
|---|---|---|
| Обводка | `white/25` | `white/12` |
| Скругление | 2px | **4px** |
| Заливка | `white/15` | `white/10` |
| Размытие фона | — | `blur(12px)` |

### Про нагрузку

`backdrop-filter` пересчитывается каждый кадр, а под плашками работает
анимированный шейдер — поэтому замерили, а не понадеялись.

Замер на 1264, dpr 1, 120 кадров, с размытием и без:

| | С размытием | Без размытия |
|---|---|---|
| Среднее на кадр | 13.33 мс | 13.34 мс |
| Медиана | 13.3 мс | 13.3 мс |
| Худший кадр | 14.4 мс | 14.4 мс |
| FPS | 75 | 75 |

Разницы нет. **Но оговорка:** дисплей 75 Гц, оба варианта упёрлись
в потолок vsync — замер доказывает, что размытие укладывается в бюджет
кадра на этой машине, но не показывает, сколько запаса осталось.
На слабом железе стоит перепроверить. Взято 12px, а не максимум.

Смягчает риск то, что шейдер уже останавливается, когда секция уходит
из вьюпорта, — значит и размытие пересчитывается только на видимой.

### Где эффект виден

Только там, где превью не закрывает плашку: поля вокруг телефона
и дашборда. На карточке с ноутбуком картинка шире плашки, и стекла
там практически не видно.


## Два акцентных красных — закрыто

Разделение из макета восстановлено. В `@theme` теперь два токена по ролям:

| Токен | Значение | Где |
|---|---|---|
| `--color-accent` | `#FF1E00` | кнопки, колонка STM в таблице, выделение текста |
| `--color-accent-ink` | `#FF4112` | акценты в заголовках, цены конкурентов, знак в логотипе |

Проверено на странице:

| Элемент | Цвет |
|---|---|
| «AI-разработки» | `#FF4112` |
| «сильное впечатление» | `#FF4112` |
| «Обсудим?» | `#FF4112` |
| Цены конкурентов в таблице | `#FF4112` |
| Кнопка CTA (заливка) | `#FF1E00` |
| Ячейка STM в таблице | `#FF1E00` |
| Текст кнопки в хедере | `#FF1E00` |

Последний пункт неочевиден, но так в макете: у белой кнопки в хедере
текст именно `#FF1E00`, а не `#FF4112` — это кнопка, а не текстовый акцент.

Попутно: в `gradient.ts` значение по умолчанию было `#ff4112` (не
использовалось, компонент всегда передаёт своё) — выровнено на `#ff1e00`,
чтобы не путало.


## Меню: починены ссылки и порядок (17.09.2026)

Было три поломки:

| Пункт | Вело | Проблема |
|---|---|---|
| Как мы работаем | `#process` | указывал на секцию этапов, не на свою |
| Этапы | `#stages` | **такой секции нет** — ссылка мертвая |
| Цены / Этапы | — | порядок в меню не совпадал с порядком секций |

Стало — пункты идут в порядке появления секций на странице:

| Пункт | Ведёт | Позиция на странице |
|---|---|---|
| Как мы работаем | `#about` | 800 |
| Кому подойдет | `#audience` | 1695 |
| Цены | `#pricing` | 2217 |
| Этапы | `#process` | 3315 |
| Наши проекты | `#works` | 4444 |

**Расхождение с макетом:** там «Этапы» стоят перед «Ценами». Но на
странице таблица с ценами идёт раньше этапов работы, и при макетном
порядке переход по меню слева направо прыгал бы назад. Поменяли местами.

Проверено: все пять целей существуют, позиции монотонно растут,
переход по клику работает (прокрутка встаёт ровно на верх секции).

Секция «Что включено в проект» (`#scope`) в меню не представлена —
в макете пять пунктов, и это осознанно.


## Поп-ап с формой контактов (17.09.2026)

В макете его нет — блок новый, тексты наши, требуют утверждения.

Собран на нативном `<dialog>`: модальное поведение, ловушка фокуса,
закрытие по Esc и подложка — из коробки, без библиотеки и своей
реализации. Плюс закрытие по клику на подложку и по крестику.

**Кнопки остались ссылками на `#contact`**, модалка вешается перехватом
клика. Если JS не выполнится, кнопка не превращается в мёртвую —
человек просто доскроллит до секции с контактами.

Поля: имя, телеграм или телефон, свободное описание задачи
и обязательное согласие на обработку персональных данных
(форма собирает имя и контакт — это персональные данные).

### Точка подключения

Адрес обработчика — `PUBLIC_FORM_ENDPOINT` в `.env`, см. `.env.example`.
Подойдёт что угодно, принимающее POST с FormData: Formspree, Getform,
Web3Forms, своя функция на Vercel/Netlify.

**Сейчас переменная пуста.** Форма в этом состоянии не отправляется
и честно пишет «Форма пока не подключена к обработчику заявок» —
ложного успеха не показывает. Сервис не подключён намеренно:
это решение заказчика, а не разработки.

### Проверено

| Сценарий | Результат |
|---|---|
| Открытие по кнопке | открывается, фокус на первом поле, хеш не меняется, страница не прокручивается |
| Пустая форма | «Заполните имя и контакт и подтвердите согласие» |
| Без обработчика | честное сообщение, ложного успеха нет |
| Обработчик недоступен | ошибка показана, кнопка разблокирована, введённые данные сохранены |
| Закрытие | Esc, крестик, клик по подложке |
| Мобильный 375 | 337px по ширине, влезает; автофокус отключён, чтобы не поднимать клавиатуру |

Триггеры: кнопка в хедере, в герое и в CTA — три штуки.
Кнопка «Написать в Telegram» осталась обычной ссылкой.


## Деплой (21.09.2026)

**Сайт:** https://leratk22.github.io/stm-ai-landing/
**Репозиторий:** https://github.com/leratk22/stm-ai-landing (публичный)

GitHub Pages, источник — GitHub Actions. Workflow `.github/workflows/deploy.yml`
собирает на Node 22 через pnpm и публикует `dist`.

### Про base

Проектный сайт на Pages отдаётся из подпапки `/stm-ai-landing`, поэтому
сборке нужен `base`. Включается флагом `GITHUB_PAGES=true` из workflow —
локально не ставится, иначе dev-сервер тоже уехал бы в подпапку.

Проверено: обычная сборка даёт `/_astro/`, сборка с флагом —
`/stm-ai-landing/_astro/`.

### Почему репозиторий пришлось открыть

Pages на бесплатном тарифе работает только с публичным репозиторием.
Репозиторий был приватным, открыт по решению заказчика 21.09.2026.
Исходники, макетные картинки и история коммитов теперь видны всем.

Альтернатива, если понадобится закрыть обратно: Vercel или Netlify
на бесплатном тарифе работают с приватными репозиториями.

### Грабли при первом запуске

Первый прогон workflow упал на `actions/configure-pages`: он стартовал
раньше, чем Pages был включён в настройках репозитория, и не нашёл сайт.
После включения Pages перезапуск прошёл без правок кода.


## Шаблоны страниц кейсов (25.09.2026)

Три страницы: `/cases/mobile-app/`, `/cases/landing/`, `/cases/saas/`.
Карточки кейсов на главной теперь ведут на них.

Каркас взят со страниц проектов atlasdesign.framer.ai — только раскладка,
без их текстов и картинок. Всё наполнение — плейсхолдеры.

### Раскладка

| Блок | Как устроен |
|---|---|
| Герой | градиент, тип и год, заголовок `type-display-md` внизу слева |
| Метаданные | 4 пары «подпись — значение» сеткой 2×2, колонки 1–4 из 12, **липкие** |
| Лента | колонки 7–12 — начинается ровно с середины, как в референсе |
| — обложка | пропорция 31:27 |
| — вводный абзац | `type-lead` |
| — разделы | «Задача», «Решение»: `type-h3` + абзацы |
| — галерея | 3 картинки 31:22, разрыв 40 |
| — итог | «Результат» + ряд из трёх цифр |
| Другие проекты | сетка и карточка **как на главной**: три колонки, «другие» встают в первые две, третья пустая. Без кнопки «Все проекты» |
| Финал | тот же CTA, что на главной, и поп-ап формы |

Сверено с референсом на 1440: лента начинается на 50.3% ширины,
занимает 49.7%, пропорции картинок совпадают.

### Как наполнять

Всё в `src/data/cases.ts`, разметку трогать не нужно. Название, тип
и год оттуда же идут в карточки на главной — одно место правки.

Плейсхолдеры описывают, что должно стоять в блоке («Вводный абзац:
о чём проект и для кого»), а заглушки картинок подписаны нужным
размером файла — вдвое больше отображаемого, под ретину:

| Картинка | Файл |
|---|---|
| Обложка | 1240×1080 |
| Галерея | 1240×880 |
| Превью в «Других проектах» | 800×936 |

Количество картинок в галерее задаётся полем `gallery`.

### Отклонения от референса

- **«Другие проекты» — `type-h2`, а не крупный дисплейный.** Сразу за
  блоком идёт CTA со своим 108px-заголовком — два гиганта подряд.
- **Абзацы ленты — белый 70%, а не 50%**, как в карточках главной:
  там пара строк, здесь несколько абзацев подряд.
- **Колонка метаданных липкая с отступом 40px**, а не 160, как у них:
  наш хедер не липкий. Если сделаем липкий хедер — отступ надо
  увеличить на его высоту.

### Попутно исправлено на живом сайте

Фавиконка и предзагрузка шрифта были прописаны без подпапки и на Pages
отдавали **404** — вкладка была без иконки. Шрифты при этом грузились:
пути в CSS Vite переписывает сам, а в разметке — нет.

Заведён `src/lib/url.ts` → `withBase()`. **Любая внутренняя ссылка
и любой файл из `public/` в разметке должны идти через него.**

Меню и логотип тоже переведены на него: со страницы кейса `#about`
вёл бы в никуда — там нет такой секции. Теперь ведёт на главную
к нужному блоку.


### Правка «Других проектов» (25.09.2026)

По замечанию заказчика раскладка из референса (карточки по краям сетки
и кнопка «Все проекты») заменена на сетку главной. Кейсов три, значит
«других» два: первые две колонки заняты, третья пуста. Кнопки нет —
отдельной страницы со всеми проектами не существует.

Чтобы блок совпадал с главной не на глаз, карточка вынесена в общий
компонент `ui/CaseCard.astro` и используется в обоих местах. Настройки
превью (картинка и то, как она вписана в плашку) переехали из секции
главной в `src/data/cases.ts` — поле `preview` у каждого кейса.

Сверено на 1440: плашки 459×436 и на главной, и на странице кейса,
первые две стоят в одних и тех же точках.


## Кейс «Платформа экомониторинга» (25.09.2026)

Первый наполненный кейс — место SAAS (`/cases/saas/`). Основа — макет
ModulCloud (Figma `UWK3HA4P4Llya6L4wmK8s7`, секция `85:2166`): карта
с датчиками → панель устройства с 13 показателями в пяти группах →
график одного показателя; настройки и статистика поверх карты;
две темы.

**Название компании не указано** — по просьбе заказчика. В заголовке
описание продукта, в поле «Клиент» — «Производитель датчиков».

Картинок пока нет, заглушки. Подписи на них теперь говорят, какой
экран куда ставить: поле `gallery` принимает список подписей.

### ⚠ Черновые данные

Придуманы по просьбе заказчика, **заменить на данные клиента до
публичного запуска**:

- срок «8 недель»;
- «Результат» целиком;
- цифра «2 мин на отчёт вместо дня сводки».

Настоящие, взяты из макета: «13 показателей на одном экране»
и «15 мин на настройку без выезда» (в окне настроек устройства
сказано, что новая конфигурация применяется в течение 15 минут).

### Типограф

`src/lib/typograph.ts` → `tp()`: неразрывный пробел перед тире,
после однобуквенных предлогов и союзов, между числом и единицей.
Применяется ко всем текстам страницы кейса — новые тексты можно
вставлять с обычными пробелами. На главной пока не применяется.


## Слайдер кейсов в герое (25.09.2026)

Плашка 414×220 из макета теперь листает превью всех трёх кейсов,
каждый слайд ведёт на страницу кейса. Компонент `ui/HeroCaseSlider.astro`,
логика `scripts/case-slider.ts`.

### Посадка картинок

Плашка широкая, а превью подогнаны под почти квадратные карточки ниже.
Для героя у каждого кейса своя посадка — поле `preview.hero` в `cases.ts`.
Правило одно для всех: устройство прижато к верху и обрезано снизу,
как телефон в макете.

| Кейс | Ширина | Что видно |
|---|---|---|
| Телефон | 56% | как в макете |
| Ноутбук | 92% | экран целиком, клавиатура уходит за край |
| Дашборд | 82% | верх интерфейса, читается как окно браузера |

### Полоса прогресса

Под стрелками, шириной ровно в их блок. Заполняется за 6 секунд,
после чего слайд листается сам.

**Таймером служит сама анимация полосы** (Web Animations API): дорастает
до конца — слайд листается. Поэтому полоса и реальный отсчёт не могут
разойтись, а пауза — это просто пауза анимации.

Пауза, когда: курсор над слайдером, фокус внутри, вкладка скрыта,
слайдер ушёл с экрана. После паузы отсчёт идёт с того же места.
Ручное листание сбрасывает полосу в ноль.

### Управление

- стрелки на экране и стрелки клавиатуры;
- свайп на тач-экранах (после свайпа клик гасится, чтобы не открылся кейс);
- по кругу в обе стороны.

### Доступность

- `role="region"` + `aria-roledescription` у карусели и слайдов;
- неактивные слайды `inert` — ссылки на них недоступны с клавиатуры;
- скринридер озвучивает «2 из 3: …» только при **ручном** листании —
  автопрокрутка каждые 6 секунд превратила бы его в поток объявлений;
- при `prefers-reduced-motion` автопрокрутки нет, смена без сдвига;
- без JS виден первый кейс, стрелки и полоса скрыты.

### Проверено

| Сценарий | Результат |
|---|---|
| Стрелки вперёд / назад, по кругу | 0 → 1, 1 → 0 → 2 |
| Автопрокрутка | через 6 с сменился слайд, полоса сбросилась |
| Наведение | за 2 с полоса не двинулась, слайд не сменился |
| Курсор ушёл | отсчёт продолжился с того же места |
| Озвучка | «2 из 3: Название проекта» |


## Правило: трансформы Tailwind 4 не смешивать с анимацией transform

Баг, найденный заказчиком 25.09.2026: полоса прогресса в слайдере
не заполнялась.

Причина. У полосы был класс `scale-x-0`. В Tailwind 4 классы `scale-*`,
`rotate-*` и `translate-*` задают **отдельные CSS-свойства** `scale`,
`rotate`, `translate`, а не `transform`. Анимация меняла
`transform: scaleX(…)`, а браузер перемножает оба свойства — итог
оставался нулевым при любом значении анимации.

Почему тест не поймал. Проверялось `getComputedStyle(bar).transform` —
оно честно росло. А на экране ничего не менялось.

Это второй раз на тех же граблях: в FAQ поворот плюса тоже сначала
«не работал» по проверке `transform`, потому что Tailwind крутил его
через `rotate`.

**Как теперь:**
1. У элементов, которые анимирует JS через `transform`, не ставить
   классы `scale-*` / `rotate-*` / `translate-*`. Исходное состояние —
   инлайном в `transform`.
2. **Проверять видимый результат, а не значение свойства:**
   `getBoundingClientRect()` учитывает все трансформы сразу.

Проверка после правки — реальная ширина полосы на дорожке 68px:

| Время | Ширина |
|---|---|
| 0 с | 0 px |
| 1.5 с | 17 px |
| 3 с | 34 px |
| 4.5 с | 51 px |

Попутно: при `prefers-reduced-motion` автопрокрутки нет, и пустая
дорожка теперь скрыта — раньше под стрелками висела бы серая линия.
